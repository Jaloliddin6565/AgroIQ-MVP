# AgroIQ MVP v0.2.0 — Python 3.11
#
# DIQQAT: bu ro'yxatda apparat (RS485/Modbus/Bluetooth) kutubxonalari YO'Q —
# shu sababli Streamlit Community Cloud deploy'i barqaror bo'lib qoladi.
# Lokal edge gateway bog'liqliklari: edge_gateway/requirements-edge.txt

streamlit>=1.50,<2.0
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
plotly>=5.20
pydantic>=2.6
reportlab>=4.1

# Gateway API rejimi uchun (ixtiyoriy — bo'lmasa demo/qo'lda/fayl rejimlari ishlaydi)
requests>=2.31

# Testlar
pytest>=8.0
pypdf>=4.0
